/**
 * TEMPORARY CONTAINMENT (Phase 1 — updater signing incident):
 * Keep false until a valid Tauri updater *public* key is configured and
 * restoration steps in docs/release-hardening/phase-1-updater-containment.md
 * are complete. Do not re-enable for empty, placeholder, or private-key material.
 */
export const UPDATER_ENABLED = false;
